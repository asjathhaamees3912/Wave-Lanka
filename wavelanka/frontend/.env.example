# Railway service URLs (baked in at Next.js build time — set in Railway Variables before deploy)
# No trailing slash, no quotes
NEXT_PUBLIC_BACKEND_URL=https://YOUR-BACKEND.up.railway.app
NEXT_PUBLIC_AI_URL=https://YOUR-AI.up.railway.app
